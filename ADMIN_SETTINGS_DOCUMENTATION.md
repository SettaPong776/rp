# เอกสารประกอบ Admin Settings Page

## 📋 ภาพรวม

ไฟล์ `admin_settings.php` เป็นหน้าเว็บสำหรับการจัดการการตั้งค่าระบบของแอปพลิเคชันแจ้งซ่อมออนไลน์ ซึ่งช่วยให้ผู้ดูแลระบบสามารถกำหนดค่าพื้นฐานของระบบและการแจ้งเตือนผ่าน Telegram ได้

## 🔐 สิทธิ์การเข้าถึง

- **บทบาท:** Admin เท่านั้น
- **ตรวจสอบ:** ตรวจสอบ Session ว่าผู้ใช้ได้ล็อกอินและมีบทบาท `admin`
- หากไม่ตรงตามเงื่อนไข จะเปลี่ยนเส้นทางไปยัง `login.php`

```php
if (!isset($_SESSION['user_id']) || $_SESSION['role'] != 'admin') {
    header('Location: login.php');
    exit();
}
```

## 📝 ฟังก์ชันหลัก

### 1. อัพเดตการตั้งค่า (`update_settings`)

**เงื่อนไข:** ตรวจสอบว่า POST request มี `update_settings`

**ข้อมูลที่อัพเดต:**
| ชื่อการตั้งค่า | คำอธิบาย |
|---|---|
| `site_name` | ชื่อระบบ |
| `site_description` | คำอธิบายระบบ |
| `telegram_bot_token` | Token สำหรับ Bot Telegram |
| `telegram_chat_id` | Chat ID สำหรับรับข้อความ |
| `notification_enabled` | เปิด/ปิดการแจ้งเตือน |

**กระบวนการ:**
1. ทำความสะอาดข้อมูล Input ด้วย `clean_input()`
2. อัพเดตแต่ละการตั้งค่าในฐานข้อมูลในตาราง `settings`
3. หากสำเร็จ: แสดงข้อความสำเร็จและส่งการแจ้งเตือนไปยัง Telegram (ถ้าเปิดใช้งาน)
4. หากล้มเหลว: แสดงข้อความผิดพลาดพร้อม SQL error

```php
if (isset($_POST['update_settings'])) {
    // ทำความสะอาดข้อมูล
    $site_name = clean_input($_POST['site_name']);
    // ...
    
    // อัพเดตในฐานข้อมูล
    $query = "UPDATE settings SET setting_value = '$value' WHERE setting_name = '$name'";
}
```

### 2. ทดสอบการเชื่อมต่อ Telegram (`test_telegram`)

**เงื่อนไข:** ตรวจสอบว่า POST request มี `test_telegram`

**ขั้นตอน:**
1. รับ `telegram_bot_token` และ `telegram_chat_id` จาก Input
2. สร้าง Request ไปยัง Telegram API: `https://api.telegram.org/bot{TOKEN}/sendMessage`
3. ส่งข้อความทดสอบ พร้อมพยายามเชื่อมต่อด้วย cURL
4. ตรวจสอบ HTTP Status Code:
   - **200:** การเชื่อมต่อสำเร็จ
   - **อื่นๆ:** แสดงข้อผิดพลาดพร้อม HTTP Code

```php
$ch = curl_init($url);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, $data);
```

### 3. ดึงข้อมูลการตั้งค่าปัจจุบัน

ดึงข้อมูลทั้งหมดจากตาราง `settings` และเก็บไว้ในอาร์เรย์ `$settings` เพื่อแสดงในฟอร์ม

```php
$query = "SELECT * FROM settings";
$result = mysqli_query($conn, $query);
while ($row = mysqli_fetch_assoc($result)) {
    $settings[$row['setting_name']] = $row['setting_value'];
}
```

## 🎨 UI Components

### แท็บการตั้งค่า 

หน้านี้มี 2 แท็บหลัก:

#### 1️⃣ ตั้งค่าทั่วไป (General Settings) - **เปิดใช้งาน**

**ฟิลด์:**
- ชื่อระบบ (`site_name`) - **จำเป็น**
- คำอธิบายระบบ (`site_description`) - ไม่จำเป็น

**UI Elements:**
- Input field พร้อมไอคอน (Boxicons)
- Bootstrap form styling
- Responsive Grid (col-md-6)

#### 2️⃣ การแจ้งเตือน (Notification) - **ซ่อน**

**ฟิลด์:**
- Telegram Bot Token
- Telegram Chat ID
- Checkbox เปิด/ปิดการแจ้งเตือน
- ปุ่ม "ทดสอบการเชื่อมต่อ"

**หมายเหตุ:** แท็บนี้ถูกซ่อนใน HTML (commented out) แต่ JavaScript ยังใช้ข้อมูลจากฟอร์มนี้

### ข้อความแจ้งเตือน (Alerts)

| ประเภท | ตัวแปร | สถานการณ์ |
|---|---|---|
| Success | `$success_message` | อัพเดตการตั้งค่าสำเร็จ |
| Error | `$error` | เกิดข้อผิดพลาดในการอัพเดต |
| Success | `$test_success` | ส่งข้อความทดสอบสำเร็จ |
| Error | `$test_error` | เกิดข้อผิดพลาดในการทดสอบ |

## ⚙️ JavaScript Functionality

### Event Listener: Save Settings Button

```javascript
document.getElementById('save-settings').addEventListener('click', function() {
    // รวบรวมข้อมูลจากทั้งสองฟอร์ม
    const form = document.createElement('form');
    form.method = 'POST';
    
    // เพิ่ม input fields
    // - site_name
    // - site_description
    // - telegram_bot_token
    // - telegram_chat_id
    // - notification_enabled (หากติ่ก checkbox)
    
    form.submit();
});
```

**วัตถุประสงค์:**
1. รวบรวมข้อมูลจากทั้ง 2 แท็บ
2. สร้างฟอร์มที่ซ่อนไว้
3. เพิ่ม `update_settings` flag
4. ส่ง Form ด้วย POST method

## 🗄️ ฐานข้อมูล

### ตาราง: `settings`

**คอลัมน์:**
| คอลัมน์ | ชนิด | คำอธิบาย |
|---|---|---|
| `setting_name` | VARCHAR | ชื่อการตั้งค่า (Primary Key) |
| `setting_value` | TEXT/VARCHAR | ค่าของการตั้งค่า |

**ตัวอย่าง Data:**
```
setting_name          | setting_value
---------------------|------------------------------------
site_name             | ระบบแจ้งซ่อมออนไลน์
site_description      | ระบบสำหรับรายงานการซ่อมบำรุง
telegram_bot_token    | 123456789:AbCdEfGhIjK...
telegram_chat_id      | 987654321
notification_enabled  | true
```

## 🔧 ฟังก์ชันหนุน (Helper Functions)

| ฟังก์ชัน | แหล่งที่มา | วัตถุประสงค์ |
|---|---|---|
| `clean_input()` | config/db_connect.php | ทำความสะอาด Input ป้องกัน SQL Injection |
| `send_telegram_notification()` | config/db_connect.php | ส่งข้อความแจ้งเตือนไปยัง Telegram |
| `thai_date()` | config/db_connect.php | แปลงวันที่เป็นรูปแบบภาษาไทย |

## ⚠️ ความเสี่ยงและจุดที่ต้องปรับปรุง

### 🚨 SQL Injection Risk
**ปัญหา:** ใช้ `$value` โดยตรงใน Query แม้มีการใช้ `clean_input()`

```php
$query = "UPDATE settings SET setting_value = '$value' WHERE setting_name = '$name'";
```

**แนวทางแก้ไข:** ใช้ Prepared Statements
```php
$stmt = $conn->prepare("UPDATE settings SET setting_value = ? WHERE setting_name = ?");
$stmt->bind_param("ss", $value, $name);
$stmt->execute();
```

### 🔐 Telegram Token Exposure
**ปัญหา:** Token แสดงอยู่ใน HTML form

**แนวทางแก้ไข:** ซ่อน Token หลังจาก input เบื้องต้น
```php
<input type="password" class="form-control" id="telegram_bot_token" ...>
```

### ✅ Session Management
**ปัญหา:** ไม่ได้ตรวจสอบ CSRF token

**แนวทางแก้ไข:** เพิ่ม CSRF protection

## 🔄 Data Flow Diagram

```
User (Admin)
    ↓
[admin_settings.php]
    ↓
┌─────────────────────┐
│ Process POST Data   │
│ - update_settings   │
│ - test_telegram     │
└─────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ Database Operations / API Calls      │
│ - UPDATE settings table             │
│ - POST to Telegram API              │
└─────────────────────────────────────┘
    ↓
┌─────────────────────┐
│ Display Alerts      │
│ - Success/Error     │
│ - Test Results      │
└─────────────────────┘
    ↓
Display Updated Settings Form
```

## 📱 การใช้งาน Telegram Integration

### ขั้นตอนการตั้งค่า

1. **สร้าง Telegram Bot**
   - ค้นหา `@BotFather` บน Telegram
   - ส่งคำสั่ง `/newbot`
   - ทำตามขั้นตอนและจะได้รับ Token

2. **ค้นหา Chat ID**
   - ค้นหา `@getidsbot` บน Telegram
   - ส่งข้อความใด ๆ
   - Bot จะส่งข้อมูล ID ของคุณกลับมา

3. **ใส่ข้อมูลในระบบ**
   - ไปยัง Admin Settings
   - ใส่ Bot Token ในช่อง "Telegram Bot Token"
   - ใส่ Chat ID ในช่อง "Telegram Chat ID"
   - คลิก "ทดสอบการเชื่อมต่อ"

4. **ตรวจสอบ**
   - จะได้รับข้อความทดสอบในแชท Telegram
   - ถ้าได้รับ = ตั้งค่าสำเร็จ

## 📚 Related Files

- [config/db_connect.php](config/db_connect.php) - Database connection และ Helper functions
- [includes/header.php](includes/header.php) - Header template
- [includes/footer.php](includes/footer.php) - Footer template
- [database_testrp.sql](database_testrp.sql) - Database schema

## 🎯 Functionality Checklist

- ✅ ตรวจสอบสิทธิ์ Admin
- ✅ อัพเดตการตั้งค่าในฐานข้อมูล
- ✅ ทดสอบการเชื่อมต่อ Telegram
- ✅ แสดงข้อความแจ้งเตือน Success/Error
- ✅ ดึงการตั้งค่าปัจจุบัน
- ✅ Responsive UI Design
- ⚠️ SQL Injection protection (ต้องปรับปรุง)
- ⚠️ CSRF protection (ยังไม่มี)

---

**Last Updated:** January 13, 2026  
**Version:** 1.0
